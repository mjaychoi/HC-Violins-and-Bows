/**
 * ⚠️ DEPRECATED: Next.js 15에서는 instrumentation-client.ts를 사용합니다
 *
 * 이 파일은 Next.js 15 + Sentry 권장 방식에 따라 비워둡니다.
 * 실제 Sentry 초기화는 src/instrumentation-client.ts에서 수행됩니다.
 *
 * Next.js가 이 파일을 찾을 수 있어야 하므로 삭제하지 않고 비워둡니다.
 */

// Sentry initialization is now in src/instrumentation-client.ts
